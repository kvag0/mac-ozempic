[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mac-ozempic"
version = "0.1.0"
requires-python = ">=3.9"
description = "macOS CLI disk cleaner"
license = {text = "MIT"}

[project.scripts]
ozempic = "ozempic.__main__:main"
